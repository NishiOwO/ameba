# $Id$
# @Id. 

include @TOP@/config.mk

.PHONY: all install clean distclean

all: amb@EXEC@

amb@EXEC@: *.pas
	$(PC) -o$@ -Fu../lib Main.pas

install: all

clean:
	rm -f amb *.exe *.o *.ppu

distclean: clean
	rm -f Makefile
