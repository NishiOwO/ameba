# $Id$
# @Id. 

include @TOP@/config.mk

.PHONY: all install clean distclean
.SUFFIXES: .pas .o

all: AmebaCore.o

.pas.o:
	$(PC) $<

install: all

clean:
	rm -f *.so *.dll *.a *.o *.ppu

distclean: clean
	rm -f Makefile
