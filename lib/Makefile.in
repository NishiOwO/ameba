# $Id$
# @Id. 

OBJS = main.o

include @TOP@/config.mk

.PHONY: all install clean distclean
.SUFFIXES: .c .o

all: libamb.a

libamb.a: $(OBJS)
	$(AR) rc $@ $(OBJS)
	$(RANLIB) $@

.c.o:
	$(CC) -c $(CFLAGS) -o $@ $<

install: all

clean:
	rm -f *.so *.dll *.a *.o

distclean: clean
	rm -f Makefile
